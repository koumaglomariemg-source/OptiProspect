{
  "name": "optiprospect",
  "version": "1.0.0",
  "private": true,
  "description": "Application de gestion de prospection commerciale moderne",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "dev": "concurrently -n API,CLIENT -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "start": "npm --prefix server run start",
    "build": "npm --prefix client run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
