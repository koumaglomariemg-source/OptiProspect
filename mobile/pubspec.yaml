name: optiprospect_app
description: "OptiProspect — gestion de prospection commerciale (mobile)."
publish_to: 'none'
version: 2.0.0+2

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.5.5
  provider: ^6.1.5
  intl: ^0.19.0
  url_launcher: ^6.3.1
  fl_chart: ^1.2.0
  image_picker: ^1.2.3
  drag_and_drop_lists: ^0.4.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
